@model shop_net.ViewModels.ImagedItem

<div class="col-lg-4">
    <img src="@Model.Images.First().Url"/>
    <h2>@Model.Name</h2>
    <p>@Model.Description</p>
    <p>Цена: @Model.Price.ToString("C")</p>
</div>